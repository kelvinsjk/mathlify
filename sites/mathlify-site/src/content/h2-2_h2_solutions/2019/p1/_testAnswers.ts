import { mathlifierDj as mathlifier } from 'mathlifier';

// q3
export const q3 = {
	parts: [
		{
			body: mathlifier`${{}} p = 2,
${{}} q = - 1,
${{}} r = - 5.`
		},
		{
			body: mathlifier`Translate the graph by ${{}} 1
unit in the positive ${{}} x\\text{-axis}
direction.\\
Translate the resulting graph by ${{}} 5
units in the negative ${{}} y\\text{-axis}
direction.\\
Scale the resulting graph by scale factor ${{}} 2
parallel to the ${{}} y\\text{-axis}.`
		}
	]
};
// q5
export const q5 = {
	parts: [
		{
			body: mathlifier`${{}} f^{-1}(x) = \\frac{\\ln \\left( x + 4 \\right)}{2}.
\\
${{}} D_{f^{-1}} = R_{f} = \\left( -4, \\infty \\right).`
		},
		{ body: mathlifier`${{}} x=\\ln 3 - 2.` }
	]
};
